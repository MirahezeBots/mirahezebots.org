{
  "private": true,
  "scripts": {
    "test": "npm run stylelint && npm run htmllint",
    "stylelint": "stylelint 'assets/*.css'",
    "htmllint": "htmllint 'templates/*.html'"
  },
  "devDependencies": {
    "htmllint": "0.7.3",
    "htmllint-cli": "^0.0.7",
    "stylelint": "^13.13.1",
    "stylelint-config-standard": "^22.0.0"
  }
}
